{
  "name": "codeant-ai-reviewer",
  "private": true,
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "frontend": "npm run dev --workspace frontend",
    "backend": "npm run dev --workspace server"
  }
}
